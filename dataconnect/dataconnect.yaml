specVersion: "v1beta"
serviceId: "netflex-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "netflex-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
